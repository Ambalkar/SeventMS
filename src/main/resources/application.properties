# Server configuration
server.port=${PORT:8080}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

# Database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/event_ms_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JSP Configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
server.servlet.jsp.registered=true

# Error Page Configuration
server.error.whitelabel.enabled=false
server.error.path=/error

# Logging Configuration
logging.level.root=INFO
logging.level.com.eventms=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR

# Spring Security Configuration
spring.security.user.name=${SPRING_SECURITY_USERNAME:admin}
spring.security.user.password=${SPRING_SECURITY_PASSWORD:admin}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10485760
spring.servlet.multipart.max-request-size=10485760

# SQL Initialization Configuration
spring.sql.init.separator=;;

# Admin Default Credentials (BCrypt hash of 'admin2026')
# IMPORTANT: BCrypt hashes contain $ signs — do NOT wrap the hash inside ${} placeholder defaults,
# as Spring's property resolver will corrupt the $ characters.
# To override: set ADMIN_EMAIL and ADMIN_PASSWORD_HASH environment variables before starting the app.
admin.email=${ADMIN_EMAIL:devendraambalkar11@gmail.com}
admin.password.hash=$2b$10$YewnGylPmc3W5O3Z8preKeWWGhpPr.kZtvqYiBrRZseDjUo1mMVmG

# Google Gemini configuration
gemini.api.key=${GEMINI_API_KEY}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent}
